import Template from 'react-module-typescript-template';

const App = () => {
  return (
    <Template>
      And this is an example of usage of the template.
    </Template>
  );
};

export default App;
